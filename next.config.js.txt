/** @type {import('next').NextConfig} */
const nextConfig = {
  // ⭐️ الإعداد الأول: لتصدير المشروع كملفات HTML ثابتة (جديد)
  output: 'export',
  
  // ⭐️ الإعداد الثاني: لتحديد مسار القاعدة ليتطابق مع اسم المستودع (جديد)
  basePath: '/TD7-PORTFOLIO', // تأكد من مطابقة اسم مستودعك بالضبط
  
  // إعداد الصور (موجود لديك)
  images: {
    unoptimized: true,
  },
};

module.exports = nextConfig;