import './globals.css';
import Link from 'next/link';

// Defines the metadata for the project
export const metadata = {
  title: 'Ar/Td7 - Portfolio Gallery',
  description: 'A high-performance portfolio showcasing designs, logos, drawings, and videos.',
};

// Simple Navigation Component
const Navbar = () => {
  const categories =;

  return (
    <nav className="bg-gray-800 p-4 shadow-lg sticky top-0 z-10">
      <div className="container mx-auto flex justify-between items-center">
        <h1 className="text-2xl font-bold text-orange-400">Ar/Td7</h1>
        <div className="space-x-4">
          {categories.map((item) => (
            <Link key={item.name} href={item.href} className="text-white hover:text-orange-300 transition duration-150 text-lg">
              {item.name}
            </Link>
          ))}
        </div>
      </div>
    </nav>
  );
};

// Root Layout Component
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body>
        <Navbar />
        <main>
          {children}
        </main>
      </body>
    </html>
  );
}